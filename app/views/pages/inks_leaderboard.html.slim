- content_for :title, "Inks Leaderboard"
div class="leaderboard"
  ol
    - LeaderBoard.inks.each do |user|
      li
        = link_to user.public_name, user_path(user)
        = " (#{user.counter})"
