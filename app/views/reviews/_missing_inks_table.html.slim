table class="fpc-review-table table table-striped"
  tr
    th Ink
  - macro_clusters.each do |macro_cluster|
    tr
      td= link_to macro_cluster.name, ink_path(macro_cluster)

= paginate @macro_clusters
